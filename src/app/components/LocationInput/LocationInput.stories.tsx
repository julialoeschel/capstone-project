import React from 'react'
import LocationInput from './LocationInput'

// test if storybook works
export default {
  title: 'Component/LocationInput',
  component: 'LocationInput',
}

export const Regular = (): JSX.Element => <LocationInput />
